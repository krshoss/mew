import { describe } from '@jest/globals'

describe('createLogger', () => {
  // Todo: Add tests for the createLogger function in index.ts
  it('should return a new MewLogger instance', () => {
    expect(true).toBe(true)
  })
})
